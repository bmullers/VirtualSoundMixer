package main.window

import main.Channel
import main.Effect
import main.Filter
import javax.swing.JLabel

fun channelViewer(channel : Channel) : JLabel{
    //TODO : How to display a channel in a label
    return JLabel()
}

fun filterViewer(filter : Filter) : JLabel{
    //TODO : How to display a filter inside a label
    return JLabel()
}

fun effectViewer(effect : Effect) : JLabel{
    //TODO : how to display an effect inside a label
    return JLabel()
}